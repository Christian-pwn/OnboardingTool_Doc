\usepackage{luaotfload}                 % Font loader
\usepackage{fontspec}                   % Font selection
\usepackage[EU2]{fontenc}               % Font encoding
\usepackage[extendedchars]{grffile}     % non-ascii characters in paths
\usepackage{lmodern}                    % text & mathematics fonts / additional characters

\newcommand\auxDir{./out}               % auxiliary directory (must be identical to the definition in the .latexmkrc file)

% ========================================
%              Configurations             
% ========================================

% --------------- language ---------------
\usepackage[english]{babel}             % english language
% \usepackage[german]{babel}              % german language

% ------------ document fonts ------------
% add custom fonts to the "fonts" folder and change the configurations below

\setmainfont{NotoSans}%             % document default font
[                                   % ┝ comment out this section to use globally installed fonts
    Path = fonts/,                  % |
    Extension = .ttf,               % |
    UprightFont = *-Regular,        % |
    BoldFont = *-Bold,              % |
    ItalicFont = *-Italic,          % |
    BoldItalicFont = *-BoldItalic   % |
]                                   % |

\setmonofont{NotoSansMono}%         % document mono font
[                                   % ┝ comment out this section to use globally installed fonts
    Path = fonts/,                  % |
    Extension = .ttf,               % |
    UprightFont = *-Regular,        % |
    BoldFont = *-Bold,              % |
]                                   % |

% ========================================
%        Enable or disable packages       
% ========================================

% ------------ preconfigured -------------
\usepackage{styles/commands}            % enable or disable predefined commands
% \usepackage{xcolor}
% \definecolor{htbla}{HTML}{148873}

\usepackage{hyperref}
% \hypersetup{colorlinks=true, allcolors=htbla}

\usepackage{geometry}
\geometry{top=20mm,bottom=25mm}


% --------- mathematical packages --------
\usepackage{amsmath}                    % mathematical symbols
\usepackage{amssymb}                    % mathematical symbols
\usepackage{amsfonts}                   % mathematical symbols

% ----------- general packages -----------
\usepackage{tocvsec2}                   % table of contents control
\usepackage{hyperref}                   % produce hypertext links
\usepackage{csquotes}                   % advanced facilities for quotations

% ----------- layout packages ------------
\usepackage{geometry}                   % modify document dimensions
\usepackage{parskip}                    % customize paragraph indent
\usepackage{calc}                       % perform arithmetic on the arguments of latex commands
\usepackage{float}                      % improved interface for defining figures and tables
% \usepackage{multicol}                   % add column environment
% \usepackage{multirow}                   % merge rows in tables
% \usepackage{subcaption}                 % add subfigures and subtables
% \usepackage{wrapfig}                    % create figures with floating text around
% \usepackage{enumitem}                   % control layout of list environments
% \usepackage{array}                      % extend options for column formats

% ----------- styling packages -----------
% \usepackage{booktabs}                   % enhance tables styles
% \usepackage{xcolor}                     % enable colour management
% \usepackage[most]{tcolorbox}            % add coloured and framed boxes environment

% ------- bibliography management --------
\usepackage[                            % ┝ easier and more flexible bibliography management
 backend=biber,                         % |
 style=iso-numeric,                     % | or iso-authoryear
]{biblatex}                             % |
\addbibresource{references.bib}         % |

% -------- source code formatting --------
% \PassOptionsToPackage{outputdir=\auxDir}{minted}\usepackage{minted}